language: node_js
node_js:
  - "7"
cache:
  directories:
    - "node_modules"
install:
  - npm install 
before_script:
  - npm install -g firebase-tools
jobs:
  include:
    - stage: test
      script:
        - npm test
    - stage: deploy
      if: branch = master
      script:
        - npm run build && firebase deploy --token $FIREBASE_TOKEN
      
